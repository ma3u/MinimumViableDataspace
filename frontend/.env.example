# Frontend Environment Configuration
# Copy this to .env.local and customize

# API Mode: mock | hybrid | full
# - mock: Direct calls to backend-mock (port 3001)
# - hybrid: Calls to backend-edc with mock data + EDC catalog/metadata
# - full: Complete EDC data flow through connectors
VITE_API_MODE=mock

# Mock Backend URL (used in mock mode)
VITE_MOCK_API_URL=http://localhost:3001

# EDC Backend URL (used in hybrid and full modes)
VITE_EDC_API_URL=http://localhost:3002

# EDC Participant Configuration (used in hybrid and full modes)
# Provider DID - Identity of the healthcare provider (e.g., Rheinland Klinikum)
VITE_PROVIDER_DID=did:web:provider-identityhub%3A7083:api:identity

# Consumer DID - Identity of the data consumer (e.g., Nordstein Research)
VITE_CONSUMER_DID=did:web:consumer-identityhub%3A7093:api:identity

# Provider DSP endpoint for catalog/negotiation/transfer
VITE_PROVIDER_DSP_URL=http://localhost:8193/api/dsp

# Enable debug logging
VITE_DEBUG=false
