---
apiVersion: v1
kind: ConfigMap
metadata:
  name: issuer-service-config
  namespace: dataspace-issuer
  labels:
    app.kubernetes.io/name: issuer-service
    app.kubernetes.io/component: credential-issuer
    app.kubernetes.io/part-of: mvd
data:
  EDC_PARTICIPANT_ID: "dataspace-issuer"
  WEB_HTTP_PORT: "10016"
  WEB_HTTP_PATH: "/api"
  EDC_IAM_DID_WEB_USE_HTTPS: "true"
  EDC_LOG_LEVEL: "INFO"
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: issuer-service
  namespace: dataspace-issuer
  labels:
    app.kubernetes.io/name: issuer-service
    app.kubernetes.io/component: credential-issuer
    app.kubernetes.io/part-of: mvd
automountServiceAccountToken: true
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: issuer-service
  namespace: dataspace-issuer
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list"]
    resourceNames:
      - issuer-credentials
      - issuer-signing-keys
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: issuer-service
  namespace: dataspace-issuer
subjects:
  - kind: ServiceAccount
    name: issuer-service
    namespace: dataspace-issuer
roleRef:
  kind: Role
  name: issuer-service
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: v1
kind: Service
metadata:
  name: issuer-service
  namespace: dataspace-issuer
  labels:
    app.kubernetes.io/name: issuer-service
    app.kubernetes.io/component: credential-issuer
    app.kubernetes.io/part-of: mvd
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 10016
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: issuer-service
    app.kubernetes.io/component: credential-issuer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: issuer-service
  namespace: dataspace-issuer
  labels:
    app.kubernetes.io/name: issuer-service
    app.kubernetes.io/component: credential-issuer
    app.kubernetes.io/part-of: mvd
    app.kubernetes.io/version: "latest"
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: issuer-service
      app.kubernetes.io/component: credential-issuer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: issuer-service
        app.kubernetes.io/component: credential-issuer
        app.kubernetes.io/part-of: mvd
        component: edc-runtime
    spec:
      serviceAccountName: issuer-service
      
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app.kubernetes.io/name
                      operator: In
                      values:
                        - issuer-service
                topologyKey: kubernetes.io/hostname
      
      containers:
        - name: issuer-service
          image: ghcr.io/ma3u/issuerservice:latest
          imagePullPolicy: Always
          
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 10001
            capabilities:
              drop:
                - ALL
          
          ports:
            - name: http
              containerPort: 10016
              protocol: TCP
          
          envFrom:
            - configMapRef:
                name: issuer-service-config
          
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: 1000m
              memory: 2Gi
          
          livenessProbe:
            httpGet:
              path: /api/check/liveness
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          
          readinessProbe:
            httpGet:
              path: /api/check/readiness
              port: http
            initialDelaySeconds: 20
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: cache
              mountPath: /var/cache
      
      volumes:
        - name: tmp
          emptyDir: {}
        - name: cache
          emptyDir: {}
